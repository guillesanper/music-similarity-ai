# Kept for backwards compatibility. pyproject.toml is the single source of
# dependencies, so this file only installs the project itself:
#
#   pip install -r requirements.txt
#
# which is equivalent to the documented command:
#
#   pip install -e .

-e .
