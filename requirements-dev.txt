# Kept for backwards compatibility. The versions live in the [dev] extra of
# pyproject.toml (pytest, ruff):
#
#   pip install -r requirements-dev.txt
#
# which is equivalent to:
#
#   pip install -e ".[dev]"

-e .[dev]
