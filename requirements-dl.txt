# Kept for backwards compatibility. The versions live in the [dl] extra of
# pyproject.toml (torch, torchaudio, transformers and friends), needed only from
# phase P10 on: CNN / siamese / triplet training and the MERT extractor.
#
#   pip install -r requirements-dl.txt
#
# which is equivalent to:
#
#   pip install -e ".[dl]"
#
# On the GPU server, install the CUDA build of torch first, choosing the index
# URL that matches the installed driver, e.g. for CUDA 12.4:
#   pip install torch==2.6.0 torchaudio==2.6.0 --index-url https://download.pytorch.org/whl/cu124

-e .[dl]
